# INT-POLICY-002: default=deny、ルールなし
# config: config_testvector.yaml
# 前提: IMSI 001010000000002 のポリシーが登録済みであること
#   default: "deny"
#   rules: []
# 動作: ルールなし → default=deny → Reject（AUTH_POLICY_DENIED）
version: 1
name: policy_default_deny_testvector
identity: "0001010000000002@wlan.mnc001.mcc001.3gppnetwork.org"
radius:
  attributes:
    called_station_id: "00-10-A4-23-19-C0:TESTSSID-01"
    nas_identifier: "Customer01"
expect:
  result: reject
